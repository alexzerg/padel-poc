# Optional Playtomic credentials, used only when DIRECT_FETCH=true or auth is enabled.
PLAYTOMIC_EMAIL=
PLAYTOMIC_PASSWORD=

# Optional routing-provider credentials. Never commit a populated .env file.
TOMTOM_API_KEY=
MAPBOX_TOKEN=
MAPBOX_REFERER=
