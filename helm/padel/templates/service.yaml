apiVersion: v1
kind: Service
metadata:
  name: {{ include "padel.fullname" . }}
  labels:
    {{- include "padel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "padel.selectorLabels" . | nindent 4 }}
